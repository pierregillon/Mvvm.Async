<Window x:Class="SimpleWpfApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels="clr-namespace:SimpleWpfApplication.ViewModels"
        Title="MainWindow" Height="350" Width="525">
    
    <Window.Resources>
        <viewModels:MainViewModel x:Key="MainViewModel"/>
    </Window.Resources>
    
    <StackPanel DataContext="{Binding Source={StaticResource MainViewModel}}" 
                Orientation="Horizontal"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">
        <TextBlock Text="{Binding Path=Message, Mode=OneWay}" Width="200"/>
        <Button Content="CLICK ME!" Command="{Binding Path=WriteMessageCommand}" Width="120"/>
    </StackPanel>
</Window>
